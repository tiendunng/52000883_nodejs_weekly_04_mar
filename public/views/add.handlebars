<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Thêm người dùng mới</title>
    <style>
        .alert {
            display: none;
        }
    </style>
</head>

<body>
    <div class="container mt-3">
        <a href="/" style="text-decoration: none;">Quay lại</a>
        <h3 class="text-success mt-4 mb-4">Thêm user mới</h3>
        <form action="/add" id="formAddProduct" method="post">
            <div class="form-group mt-3">
                <label for="name">Tên user</label>
                <input class="form-control mt-1" type="text" name="name" id="name" placeholder="Nhập tên người dùng"
                    required value="{{oldData.name}}">
            </div>

            <div class="form-group mt-3">
                <label for="gender">Giới tính</label>
                <select name="gender" id="gender" class="form-select mt-1" aria-valuenow="{{oldData.gender}}">
                    <option value="male">Nam</option>
                    <option value="female">Nữ</option>
                </select>
            </div>

            <div class="form-group mt-3">
                <label for="email">Email</label>
                <input class="form-control mt-1" type="email" name="email" id="email"
                    placeholder="Nhập email người dùng" required value="{{oldData.email}}">
            </div>

            <div class="form-group mt-3 mb-3">
                <label for="status">Trạng thái</label>
                <select name="status" id="status" class="form-select mt-1" aria-valuenow="{{oldData.status}}">
                    <option value="active">Active</option>
                    <option value="inactive">Inactive</option>
                </select>
            </div>

            <div class="alert alert-danger alert-dismissible mb-3">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-hidden="true"></button>
                <div id="mess">
                    {{message}}
                </div>
            </div>

            <button class="btn btn-success" type="submit">Thêm người dùng</button>
        </form>
    </div>
</body>

<script src="/js/add.js"></script>

</html>